---
name: novena-gateway-runtime
description: Use for Novena Gateway runtime work, connector behavior, local config files, install/service scripts, payload formatting, remote config, RPC handlers, attribute sync, offline buffering, and gateway-side tests.
---

# Novena Gateway Runtime

Read references/gateway_runtime.md before modifying Gateway runtime behavior, connectors, config files, service/install scripts, or tests.

Prefer focused Gateway unit tests and compile checks. Do not assume the full dependency set is installed in WSL unless verified.
