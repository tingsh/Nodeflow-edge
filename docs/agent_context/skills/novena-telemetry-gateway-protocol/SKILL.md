---
name: novena-telemetry-gateway-protocol
description: Use for Gateway changes involving MQTT topics, telemetry payload shape, payload formatter behavior, deviceId/device_id propagation, RPC/config/attribute topics, and Hub compatibility.
---

# Novena Telemetry Gateway Protocol

Read references/gateway_protocol.md before changing payload formatter behavior, MQTT publishing, Modbus uplink conversion, remote config, RPC, or attribute handling.

The Hub-side companion skill has the same name in Novena Hub and covers cloud ingestion/UI behavior.
